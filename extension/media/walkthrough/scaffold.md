## Scaffold a resource

The wizard asks for the resource name, its fields, and its operations, then
produces the spec the deterministic scaffolder wants — so the model never has to
guess it, and you review a **field table** rather than re-reading prose to check
whether `Amount` ended up `float64` or `string`.

You see every file before any of them is written.
