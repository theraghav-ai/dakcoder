-- {{.R.Table}} — schema for the {{.R.Title}} resource.
--
-- Generated by gotools resource_scaffold. Review before applying:
--
--   psql -U <user> -d <database> -f db/{{.R.TableFileStem}}.sql
--
-- The agent deliberately does not run this. Applying DDL is the one action in
-- the workflow that `git restore` cannot undo.

CREATE TABLE IF NOT EXISTS public.{{.R.Table}} (
	id serial4 NOT NULL,
{{- range .R.Fields}}
	{{.DB}} {{.SQL}},
{{- end}}
	created_at timestamp DEFAULT now() NOT NULL,
	updated_at timestamp DEFAULT now() NOT NULL,
	CONSTRAINT {{.R.Table}}_pkey PRIMARY KEY (id)
);
{{- if .R.ListFilters}}

-- Indexes for the columns the list endpoint filters on. Without these, every
-- filtered request is a sequential scan.
{{- range .R.ListFilters}}
CREATE INDEX IF NOT EXISTS idx_{{$.R.Table}}_{{.DB}} ON public.{{$.R.Table}} ({{.DB}});
{{- end}}
{{- end}}
